{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/react-learning-platform/package.json",
                "localRoot": "${workspaceFolder}/react-learning-platform"
            }
        }
    ]
}